---
- hosts: ec2-52-201-131-233.compute-1.amazonaws.com
  become: true
  become_method: sudo
  tasks:
   - name : pwd
     shell: "pwd"

 #  - name : file module
    # shell: "mkdir -p /opt/ansible/b"
 
