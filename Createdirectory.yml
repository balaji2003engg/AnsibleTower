---
- hosts: tag_Name_role_web
  become_method: sudo
  tasks:
   - name : run date command
     shell: "date"

   - name : file module
     file:
       path: "/root/ansible"
       state: directory
 
