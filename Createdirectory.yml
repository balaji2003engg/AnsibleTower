---
- hosts: ec2-34-207-116-72.compute-1.amazonaws.com
  become: true
  become_method: sudo
  tasks:
   - name : pwd
     shell: "pwd"

   - name : file module
    # shell: "mkdir -p /opt/ansible/b"
 
